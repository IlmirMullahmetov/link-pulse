# Link Pulse — Monorepo

## 📦 Структура
- `/link-pulse-front-end` — Клиент (Next.js + TypeScript)
- `/link-pulse-back-end` — Сервер (NestJS + Prisma)

---

## 🚀 Frontend

```bash
cd link-pulse-front-end
npm install          # Установка зависимостей
npm run dev          # Запуск dev-сервера
npm run build        # Продакшн сборка
npm start            # Запуск продакшн
```

## 🚀 Backend

```bash
cd link-pulse-front-end
npm install          # Установка зависимостей
npm run dev          # Запуск dev-сервера
npm run build        # Продакшн сборка
npm start            # Запуск продакшн
```

## 🚀 Качество кода

npm run lint         # ESLint
npm run lint --fix   # ESLint с автоисправлением
npm run format       # Prettier проверка
npm run format:fix   # Prettier автоформатирование
npm run ts           # Проверка типов TypeScript